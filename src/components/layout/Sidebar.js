import React from "react";
import { NavLink } from "react-router-dom";

const SideBar = () => {
  return (
    <nav id="nav-side">
      <ul>
        <li>1.</li>
      </ul>
    </nav>
  );
};

export default SideBar;
